using UnityEngine;
using System.Collections;

public class SceneUI : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//var playButton = UIButton.create("start.png", "start-selected.png", 0, 0);
	}
	
	// Update is called once per frame
	void Update () {

	}
}
